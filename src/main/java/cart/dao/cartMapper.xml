<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <!-- 다른 mapper와 중복되지 않도록 네임스페이스 기재 -->
 <mapper namespace="cartSQL">
 
 	<insert id="insert">
 		insert into cart(cart_id, userid, product_id, amount, product_name, price) values(seq_cart.nextval, #{userid}, #{product_id}, #{amount},#{product_name},#{price})
 	</insert>
 	
 	<select id="cartList" resultType="cart">
 		select product_id, amount, product_name, price from cart where userid=#{userid}
 	</select>
 	
	<select id="checkCart" resultType="Integer">
		select 	count(1) 
		from 	cart
		where	1			= 1
		and		userid 		= #{userid}
		and		product_id	= #{product_id}
 	</select>
 	
 </mapper>