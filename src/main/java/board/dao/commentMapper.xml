<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="commentSQL">
 
 	<insert id="commentWrite" parameterType="comment">
 		insert into radiantcomment(commentseq, boardseq, commentId, commenttext) values(seq_radiantcomment.next_val, #boardSeq, #{commentId}, #{commentText})
 	</insert>
 	
 	<select id="commentList" resultType="comment">
 		select boardSeq, commentid, commenttext, commentdate from radiantcomment
 	</select>
 </mapper>